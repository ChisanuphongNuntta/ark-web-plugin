# HeartShop Plugin API Abuse Filter

[Definition]
failregex = ^<HOST> .* "/api/plugin" .* (401|403|429) .*$
            ^<HOST> .* invalid.*api.*key.*$
            ^<HOST> .* plugin.*authentication.*failed.*$

ignoreregex =
