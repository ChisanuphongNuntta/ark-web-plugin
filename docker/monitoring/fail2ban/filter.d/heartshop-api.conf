# HeartShop API Abuse Filter

[Definition]
failregex = ^<HOST> .* "(GET|POST|PUT|DELETE|PATCH)" .* (429|503) .*$
            ^<HOST> .* rate.*limit.*exceeded.*$
            ^<HOST> .* too.*many.*requests.*$

ignoreregex =
