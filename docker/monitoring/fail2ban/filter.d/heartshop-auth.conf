# HeartShop Authentication Failure Filter

[Definition]
failregex = ^<HOST> .* "(POST)" .*/api/auth.* (401|403) .*$
            ^<HOST> .* authentication.*failed.*$
            ^<HOST> .* invalid.*password.*$
            ^<HOST> .* invalid.*token.*$
            ^<HOST> .* unauthorized.*$

ignoreregex =
